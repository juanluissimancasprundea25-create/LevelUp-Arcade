# ============================================================
# Configuración general de LevelUp Arcade
# Valores específicos de entorno van en application-{profile}.yml
# ============================================================
spring:
  application:
    name: LevelUp Arcade

  # --- Datasource (perfil por defecto: desarrollo local) ---
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:levelup_arcade}
    username: ${DB_USER:levelup_user}
    password: ${DB_PASSWORD:levelup_pass_2026}
    driver-class-name: org.postgresql.Driver

  # --- JPA / Hibernate ---
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
    open-in-view: false

  # --- Flyway: migraciones automáticas ---
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  # --- Thymeleaf ---
  thymeleaf:
    cache: false

# --- JWT ---
jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

server:
  port: 8080
  error:
    include-message: always

# --- Logging ---
logging:
  level:
    root: INFO
    com.leveluparcade: DEBUG
    org.springframework.security: INFO
